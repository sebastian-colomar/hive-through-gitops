apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: provider-aws-helm-odds
  namespace: openshift-gitops
spec:
  clusterSets:
    - provider-aws-helm
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions:
            - key: name
              operator: In
              values:
                - provider-aws-helm-1
                - provider-aws-helm-3

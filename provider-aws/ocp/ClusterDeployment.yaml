apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  finalizers:
    - hive.openshift.io/deprovision
  labels:
    cloud: AWS
    cluster.open-cluster-management.io/clusterset: managed
    hive.openshift.io/cluster-platform: aws
    hive.openshift.io/cluster-region: ap-south-1
    region: ap-south-1
    vendor: OpenShift
  name: provider-aws-ocp
  namespace: provider-aws-ocp
spec:
  baseDomain: sebastian-colomar.com
  clusterName: provider-aws-ocp
  controlPlaneConfig:
    servingCertificates: {}
  installAttemptsLimit: 1
  platform:
    aws:
      credentialsSecretRef:
        name: provider-aws-ocp-aws-creds
      region: ap-south-1
  provisioning:
    imageSetRef:
      name: img4.12.6-multi-appsub
    installConfigSecretRef:
      name: provider-aws-ocp-install-config
    sshPrivateKeySecretRef:
      name: provider-aws-ocp-ssh-private-key
  pullSecretRef:
    name: provider-aws-ocp-pull-secret

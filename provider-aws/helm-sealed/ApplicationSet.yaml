apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: hive-through-gitops
  namespace: openshift-gitops
spec:
  generators:
  - list:
      elements:
      - cluster: helm-sealed-1
  syncPolicy:
    preserveResourcesOnDeletion: true
  template:
    metadata:
      labels:
        applicationset: hive-through-gitops
      name: provider-aws-{{ cluster }}
    spec:
      destination:
        namespace: provider-aws-{{ cluster }}
        server: 'https://kubernetes.default.svc'
      project: default
      source:
        helm:
          valueFiles:
          - values.{{ cluster }}.yaml
        path: provider-aws/helm-sealed
        repoURL: git@github.com:sebastian-colomar/hive-through-gitops.git
        targetRevision: HEAD
      syncPolicy:
        automated:
          prune: true
          selfHeal: false
        syncOptions:
        - ApplyOutOfSyncOnly=true
        - CreateNamespace=true
